//practice 1 ---------->

/*function calculateMoney(ticketSale) {
    
    const guardFee = 500;
    const staffLunchFee = 8 * 50;

    if (0 <= ticketSale) {
        const totalEarn = ticketSale * 120;
        const totalCost = guardFee + staffLunchFee;
        const remainingBalance = totalEarn - totalCost;
        return remainingBalance;
    }
    else {
        return "Invalid Number"
    }
}
 */



//practice 2 ------------>

/*function checkName(name) {
    
    if (typeof name !== "string") {
        return "Invalid";
    }

    const lastChar = name[name.length - 1].toLowerCase();

    if (["a", "y", "i", "e", "o", "u", "w"].includes(lastChar)) {
        return "Good Name";
    } else {
        return "Bad Name";
    }
}
*/

//practice 3 ------------>



